server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=${DATASOURCE_URL:jdbc:mysql://turntable.proxy.rlwy.net:35708/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DATASOURCE_USERNAME:root}
spring.datasource.password=${DATASOURCE_PASSWORD:cNyiHMoKhAYuSgZKyUyFwKWGfglwerkc}

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:simranjn54@gmail.com}
spring.mail.password=${MAIL_PASSWORD:mkce rbks rmhe zjcd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Application Properties
app.frontend.urls=${FRONTEND_URLS:http://localhost:3000,http://localhost:3001,https://misinvoicing.netlify.app}
app.frontend.base-url=${FRONTEND_BASE_URL:http://localhost:3000}